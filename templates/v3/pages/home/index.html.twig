{% extends 'v2/base.html.twig' %}


{% block videofon %}
    {{ include('v2/blocks/section/hero.html.twig',{ h1 : block('h1') ?? page.h1 }) }}
{% endblock %}

{% block breadcrumbs %}{% endblock %}

{% block body %}

    {{ include('elements/_block_content_top_home.html.twig', {'text': page.text, 'textImg':page.textImg}) }}
    {{ partners_index() }}

{% endblock %}


{% block extraModules %}
    {% embed 'v2/embed/black-services-block.html.twig' %}
        {% block content %}
            {{ price_list(page) }}

            {#Марки модели#}
            {#{{ brands_service_block() }}#}

            {{ special_offers(page.brandAndModelName) }}
            {{ include('v2/blocks/section/advantages-home.html.twig') }}
            {# работает просто пока закомментировала #}
            {#{{ before_after(page ?? null) }}#}
            {{ include('v2/blocks/section/instagram-block.html.twig') }}
        {% endblock %}
    {% endembed %}
    {{ include('elements/instagram_widget.html.twig') }}


    {{ our_works_slider(page) }}

    {# Блок мастеров #}
    {#{{ include('v2/blocks/section/team.html.twig') }}#}


    {#{{ include('v2/blocks/section/reviews.html.twig') }}#}
{% endblock %}
